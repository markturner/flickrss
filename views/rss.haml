!!! XML
%rss{:version => "2.0", "xmlns:atom" => "http://www.w3.org/2005/Atom"}
  %channel
    %title USER's Flickr photosets
    %link http://flickr.com/photos/USERNAME/sets/
    %description Photosets by USER
    <atom:link href="your site's RSS URL" rel="self" type="application/rss+xml" />
    - @array.each do |set|
      %item
        %title= set[:title]
        %link= set[:photoset_link_url]
        %description
          = set[:description]
          %br
          %a{:href => set[:photoset_link_url]}View Photoset
          == (#{set[:count]} photos)
